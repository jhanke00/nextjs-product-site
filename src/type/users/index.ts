export type User = {
  id: number;
  firstName: string;
  lastName: string;
  phoneNumber: string;
  email: string;
};

export type UserInfo = {
  id: string;
  firstName: string;
  lastName: string;
  phoneNumber: string;
  email: string;
};
